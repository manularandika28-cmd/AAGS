{
  "name": "aags-governance-system",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix interface",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix interface\"",
    "build": "npm run build --prefix interface",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
